spring:
  application:
    name: storage-service
  config:
    import: nacos:storage-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml