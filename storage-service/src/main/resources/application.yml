spring:
  datasource:
    url: jdbc:mysql://localhost:3306/storagedb?serverTimezone=UTC
    username: root
    password: zztxjy620
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: storage-service
  config:
    import: nacos:storage-service-dev.yml
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yml
server:
  address: 0.0.0.0
